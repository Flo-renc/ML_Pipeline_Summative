# FastAPI
fastapi>=0.111.0
uvicorn[standard]

# Database
SQLAlchemy>=2.0
aiomysql==0.2.0
asyncmy
databases[mysql]
cryptography

# Pydantic v2 (required by modern FastAPI — v1 is no longer supported)
pydantic>=2.7.0

# TensorFlow CPU (no GPU on Render — saves ~500MB and avoids CUDA errors)
# Do NOT pin keras separately: TF 2.16+ installs the correct Keras 3 automatically
tensorflow-cpu==2.16.1

# OpenCV
opencv-python-headless

# Numerics
numpy==1.26.4
scipy
Pillow
pandas
scikit-learn

# Utilities
tqdm
python-multipart
protobuf>=4.21.0,<5.0.0
typing_extensions>=4.9.0
h5py